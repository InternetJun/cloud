<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example.web.mapper.UserMapper">
    <sql id="Base_Column_List">
        id, username, password, phone, account_non_locked
    </sql>
    <resultMap id="BaseResultMap" type="org.example.web.pojo.UserInfo">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="account_non_locked" property="accountNonLocked" jdbcType="TINYINT"/>
        <collection property="roles" ofType="map" column="id" select="selectRolesByUserId">
        </collection>
    </resultMap>
    <select id="selectUser" resultMap="BaseResultMap" parameterType="org.example.web.pojo.UserInfo">
        <include refid="Base_Column_List"/>
        from t_user
        where username = #{username}
    </select>
</mapper>